pipeline {
    agent any
    stages {
        stage('Lint') { 
            agent {
                dockerfile true
            }
            steps {
                sh 'pylint --rcfile=pylintrc $(find . -iname "*.py")'
            }
        }
    }
}